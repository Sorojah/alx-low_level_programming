#ifndef ABS_H
#define ABS_H

/**
 * ABS - A function-like macro that computes the absolute value of a number.
 * @x: The number to compute the absolute value of.
 * Return: The absolute value of x.
 */
#define ABS(x) ((x) < 0 ? -(x) : (x))

#endif /* ABS_H */
